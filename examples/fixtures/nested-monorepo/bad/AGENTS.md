# Nested Monorepo Instructions

## Verification

```bash
npm test
```

Run root verification before submitting changes. Packages with local runtime or deployment rules should add their own `AGENTS.md`.
